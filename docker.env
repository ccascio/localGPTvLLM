# Docker environment configuration
# Set this to use vLLM instance running on host
# Note: Using Docker gateway IP instead of host.docker.internal for Linux compatibility
VLLM_HOST=http://172.18.0.1:8000

# Alternative: Use containerized vLLM (uncomment and configure your vLLM container)
# VLLM_HOST=http://vllm:8000

# vLLM API Key (if required)
# VLLM_API_KEY=your-api-key

# For backward compatibility
OLLAMA_HOST=${VLLM_HOST}

# Other configuration
NODE_ENV=production
NEXT_PUBLIC_API_URL=http://localhost:8000
RAG_API_URL=http://rag-api:8001   